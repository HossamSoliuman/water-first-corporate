@extends('layouts.app')

@section('content')
    <section class="interior-hero flow-lines py-10 md:py-14"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><x-breadcrumbs :items="[['name' => 'Careers']]" /><div class="mt-6 grid gap-x-8 gap-y-5 lg:grid-cols-12"><div class="lg:col-span-12"><h1 class="text-[clamp(1.75rem,3.6vw,3rem)] font-semibold leading-[1.05] text-ink-800">{{ $hero['heading'] }}</h1><p class="mt-5 max-w-3xl text-lg leading-8 text-ink-500">{{ $hero['tagline'] }}</p></div></div></div></section>
    <section class="bg-white py-14 md:py-16"><div class="mx-auto grid max-w-7xl gap-12 px-4 sm:px-6 lg:grid-cols-12 lg:px-8"><div class="lg:col-span-8"><h2 class="text-3xl font-semibold leading-tight text-ink-800 md:text-4xl">{{ $intro['heading'] }}</h2><div class="mt-7 grid gap-5 text-base leading-8 text-ink-500">@foreach ([$intro['body_1'], $intro['body_2'], $intro['body_3']] as $paragraph)@if ($paragraph)<p>{{ $paragraph }}</p>@endif @endforeach</div></div><aside class="lg:col-span-4"><div class="technical-rule bg-surface p-6"><p class="font-mono text-[10px] uppercase tracking-[.15em] text-primary-600">Recruitment contact</p>@if ($contactEmail)<a href="mailto:{{ $contactEmail }}" class="mt-4 block break-all text-sm font-semibold text-primary-600">{{ $contactEmail }}</a>@endif @if ($phone)<a href="tel:{{ $phone }}" class="mt-2 block text-sm font-semibold text-primary-600">{{ $phone }}</a>@endif</div></aside></div></section>
    <section class="border-y border-ink-200 bg-surface py-14 md:py-16"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="grid gap-10 lg:grid-cols-12"><div class="lg:col-span-9"><h2 class="text-3xl font-semibold text-ink-800">{{ $jobsSection['heading'] }}</h2><p class="mt-4 max-w-2xl text-sm leading-7 text-ink-500">{{ $jobsSection['subheading'] }}</p>@if ($jobs->count())<div class="mt-9 grid gap-5 md:grid-cols-2">@foreach ($jobs as $index => $job)<article class="wf-card p-6"><span class="section-index">{{ str_pad($index + 1, 2, '0', STR_PAD_LEFT) }}</span><h3 class="mt-5 text-lg font-semibold text-ink-800">{{ $job->position_name }}</h3><div class="mt-4 flex flex-wrap gap-4 font-mono text-[9px] uppercase tracking-wider text-ink-500"><span>{{ $job->location }}</span><span class="text-accent-700">{{ $job->employment_type_label }}</span></div></article>@endforeach</div>@else<div class="technical-rule mt-8 bg-white p-7 text-sm text-ink-500">There are no open roles at present. Send a concise expression of interest to our recruitment contact.</div>@endif</div></div></div></section>
    <section class="bg-white py-14 md:py-16"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><h2 class="text-3xl font-semibold text-ink-800">{{ $whySection['heading'] }}</h2><p class="mt-4 max-w-2xl text-sm leading-7 text-ink-500">{{ $whySection['subheading'] }}</p><div class="mt-9 grid gap-5 md:grid-cols-2 lg:grid-cols-3">@foreach ($whyCards as $index => $card)<article class="wf-card p-6"><span class="section-index">{{ str_pad($index + 1, 2, '0', STR_PAD_LEFT) }}</span><h3 class="mt-5 text-lg font-semibold text-ink-800">{{ $card['title'] }}</h3><p class="mt-3 text-sm leading-6 text-ink-500">{{ $card['body'] }}</p></article>@endforeach</div></div></section>
@endsection
